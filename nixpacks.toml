# nixpacks.toml — Railway build configuration
# Pins Node 20 LTS. PDF generation uses PDFKit (pure Node.js) —
# no Chromium, no browser, no special system packages required.

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --omit=dev"]

[start]
cmd = "node server.js"
