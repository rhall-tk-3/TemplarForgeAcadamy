# Server
PORT=3000
NODE_ENV=production
BASE_URL=http://localhost:3000
LOG_LEVEL=info
ENABLE_RESOURCE_API=true

# Database metadata (optional for current static curriculum site)
DB_CLIENT=sqlite
DB_PATH=./database/ktkc-curriculum.sqlite

# Deployment
APP_NAME=ktkc-school-programs
